const dbOptions = {
    database: 'delilah_resto_localdb',
    username: 'root',
    password: null,
    dialect: 'mysql',
}

const signature = 'H3sOYaM';

module.exports = {
    dbOptions,
    signature
}